# TODO

- [ ] Implement shared language toggle logic in `js/scripts.js` that swaps text between EN (default) and PT using data attributes.
- [ ] Refactor `index.html`, `contact.html`, `projects.html`, and `resume.html` to default to EN text and reference translation keys without inline HTML fragments.
- [ ] Ensure navigation toggle/selector exists across all pages with consistent markup and styles.
- [ ] Persist language choice (localStorage) and apply on load across pages.
- [ ] Smoke-test in browser (manual) to confirm text switches without leaking HTML tags.
